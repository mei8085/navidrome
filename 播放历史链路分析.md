# Navidrome 播放历史写入与展示链路深度分析

## 一、核心数据模型

### 1. annotation 表（用户标注表）
存储用户对媒体资源的个性化标注（播放计数、最后播放时间、评分、收藏等）

**关键字段**：
- `user_id`：用户ID
- `item_id`：资源ID（歌曲/专辑/艺术家ID）
- `item_type`：资源类型（media_file/album/artist）
- `play_count`：播放次数
- `play_date`：**最后一次播放时间**（核心！用于最近播放排序）
- `rating`：评分
- `starred`：是否收藏
- `starred_at`：收藏时间

**唯一性约束**：`(user_id, item_id, item_type)` 联合唯一，每个用户对每个资源只有一条记录。

**数据结构定义**：[annotation.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/model/annotation.go#L5-L12)

### 2. scrobbles 表（播放历史表）
存储完整的播放历史记录（需 `EnableScrobbleHistory` 配置开启）

**关键字段**：
- `media_file_id`：歌曲ID
- `user_id`：用户ID
- `submission_time`：播放提交时间（Unix时间戳）

**特点**：无唯一性约束，每次有效播放都会新增一条记录。

**数据结构定义**：[scrobble.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/model/scrobble.go#L5-L9)

**表结构创建**：[20251206013022_create_scrobbles_table.sql](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/db/migrations/20251206013022_create_scrobbles_table.sql#L3-L15)

**配置项**：`EnableScrobbleHistory` - [configuration.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/conf/configuration.go#L119)

### 3. scrobble_buffer 表（外部 Scrobble 缓冲表）
存储待发送到外部 Scrobble 服务（如 Last.fm）的播放记录

**关键字段**：
- `service`：服务名称（如 "lastfm"）
- `user_id`：用户ID
- `media_file_id`：歌曲ID
- `play_time`：播放时间
- `enqueue_time`：入队时间

**数据结构定义**：[scrobble_buffer.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/model/scrobble_buffer.go#L5-L23)

**存储实现**：[scrobble_buffer_repository.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/scrobble_buffer_repository.go)

---

## 二、播放历史写入链路

### 整体流程图

```
用户播放歌曲
    ↓
ReportPlayback API 报告播放状态
    ↓
状态变为 StateStopped 且播放进度 > 阈值（50% 或 4分钟）
    ↓
调用 incPlay() 函数（事务内执行）
    ├─→ 更新 MediaFile annotation（play_count+1, play_date=max(旧值, 新时间)）
    ├─→ 更新 Album annotation（play_count+1, play_date=max(旧值, 新时间)）
    ├─→ 更新 Artist annotation（play_count+1, play_date=max(旧值, 新时间)）
    └─→ [EnableScrobbleHistory=true] 插入 scrobbles 表记录
    ↓
发送 RefreshResource 事件通知前端刷新
    ↓
dispatchScrobble → 发送到外部 Scrobble 服务（通过 scrobble_buffer 缓冲）
```

### 1. 播放状态上报入口

**API 端点**：
- Subsonic API: `scrobble.view` - [media_annotation.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/server/subsonic/media_annotation.go#L158-L185)
- Native API: `reportPlayback` - [media_annotation.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/server/subsonic/media_annotation.go#L231-L290)

**核心处理函数**：`ReportPlayback` - [play_tracker.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/play_tracker.go#L261-L382)

**播放完成判定逻辑**（第 324-341 行）：

> **⚠️ 重要：此阈值仅适用于 Native API 的 `StateStopped` 路径，Subsonic Submit 不走这个检查！**

```go
case StateStopped:
    if !params.IgnoreScrobble && player.ScrobbleEnabled {
        // 计算阈值：歌曲时长的 50% 或 240秒（4分钟），取较小值
        threshold := min(trackDurationMs*50/100, 240_000)
        if params.PositionMs >= threshold {
            // 有效播放，更新播放计数
            err = p.incPlay(ctx, mf, now)
            p.dispatchScrobble(ctx, mf, now)  // 发送到外部 scrobble 服务（Last.fm等）
        }
    }
```

**阈值适用范围对比**：

| 接口路径 | 阈值检查 | ScrobbleEnabled 控制 |
|---------|---------|-------------------|
| Native API `ReportPlayback(StateStopped)` | ✅ 50% 或 4分钟 | ✅ 本地标注 + 外部 Scrobble 都受控制 |
| Subsonic `scrobble.view?submission=true` | ❌ 无阈值，直接写入 | ❌ 本地标注**不受控制**，仅外部 Scrobble 受控制 |

### 2. 播放计数更新逻辑

**核心函数**：`incPlay` - [play_tracker.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/play_tracker.go#L434-L455)

```go
func (p *playTracker) incPlay(ctx context.Context, track *model.MediaFile, timestamp time.Time) error {
    return p.ds.WithTx(func(tx model.DataStore) error {
        // 1. 更新歌曲播放计数
        err := tx.MediaFile(ctx).IncPlayCount(track.ID, timestamp)
        // 2. 更新专辑播放计数
        err = tx.Album(ctx).IncPlayCount(track.AlbumID, timestamp)
        // 3. 更新艺术家播放计数（支持多位艺术家）
        for _, artist := range track.Participants[model.RoleArtist] {
            err = tx.Artist(ctx).IncPlayCount(artist.ID, timestamp)
        }
        // 4. 记录 scrobble 历史（如果开启）
        if conf.Server.EnableScrobbleHistory {
            return tx.Scrobble(ctx).RecordScrobble(track.ID, timestamp)
        }
        return nil
    })
}
```

### 3. IncPlayCount 实现（关键！去重逻辑）

**位置**：[sql_annotations.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/sql_annotations.go#L116-L137)

```go
func (r sqlRepository) IncPlayCount(itemID string, ts time.Time) error {
    // 先尝试更新
    upd := Update(annotationTable).Where(r.annId(itemID)).
        Set("play_count", Expr("play_count+1")).
        // 关键：只有当新时间戳大于旧值时才更新 play_date
        Set("play_date", Expr("max(ifnull(play_date,''),?)", ts))
    c, err := r.executeSQL(upd)

    // 如果没有更新到记录（说明是第一次播放），则插入新记录
    if c == 0 || errors.Is(err, sql.ErrNoRows) {
        values := map[string]any{
            "user_id":     userID,
            "item_type":   r.tableName,
            "item_id":     itemID,
            "play_count":  1,
            "play_date":   ts,
        }
        ins := Insert(annotationTable).SetMap(values)
        _, err = r.executeSQL(ins)
    }
    return err
}
```

**为什么用 max() 而不是直接覆盖？**（详细分析见第七章）

### 4. Scrobble 历史记录

**位置**：[scrobble_repository.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/scrobble_repository.go#L24-L34)

```go
func (r *scrobbleRepository) RecordScrobble(mediaFileID string, submissionTime time.Time) error {
    userID := loggedUser(r.ctx).ID
    values := map[string]any{
        "media_file_id":   mediaFileID,
        "user_id":         userID,
        "submission_time": submissionTime.Unix(),
    }
    insert := Insert(r.tableName).SetMap(values)
    _, err := r.executeSQL(insert)
    return err
}
```

---

## 三、重复播放与去重的协调机制

### 双轨制设计

Navidrome 采用**双轨制**来同时满足"完整播放历史记录"和"最近播放列表展示"的需求：

| 维度 | annotation 表 | scrobbles 表 |
|------|-------------|-------------|
| **用途** | 最近播放列表展示、播放计数 | 完整播放历史统计、数据分析 |
| **去重方式** | 天然去重（用户+资源唯一） | 不做去重 |
| **记录粒度** | 每用户每资源一条记录 | 每次播放一条记录 |
| **时间字段** | `play_date` = 最后一次播放时间 | `submission_time` = 本次播放时间 |
| **重复播放处理** | `play_count` 累加，`play_date` 取最大值 | 直接插入新记录 |
| **展示场景** | 最近播放专辑/歌曲列表 | 播放历史详情（当前未直接暴露API） |
| **API 暴露** | 通过 REST API 暴露，支持查询和排序 | 无直接查询 API |

### 关键协调点

1. **play_date 的 max() 语义**：
   - SQL 表达式：`max(ifnull(play_date,''), ?)`
   - 含义：只有当新播放时间 **晚于** 旧的 `play_date` 时才更新
   - 效果：`play_date` 始终记录 **最后一次** 播放时间

2. **时序保护**：
   - 测试用例验证：[mediafile_repository_test.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/mediafile_repository_test.go#L293-L316)
   - 场景：先播放时间 T1，再播放 T0（T0 < T1），`play_date` 保持为 T1，不会被更早的时间覆盖
   - 意义：防止客户端上报的乱序时间戳破坏最近播放排序

3. **事务一致性**：
   - 歌曲、专辑、艺术家的 annotation 更新在同一个事务中
   - annotation 更新和 scrobble 记录也在同一个事务中
   - 保证数据一致性

4. **层级联动更新**：
   - 播放歌曲时，同时更新歌曲、专辑、艺术家的播放计数
   - 支持多位艺术家（通过 Participants 字段）

### 重复播放示例

假设用户按以下顺序播放歌曲 A：
1. 2024-01-01 10:00 播放歌曲 A → 完整播放
2. 2024-01-01 11:00 播放歌曲 A → 完整播放
3. 2024-01-01 12:00 播放歌曲 A → 完整播放

**annotation 表结果**：
| user_id | item_id | item_type | play_count | play_date |
|---------|---------|-----------|------------|-----------|
| u1      | songA   | media_file| 3          | 2024-01-01 12:00 |

**scrobbles 表结果**（EnableScrobbleHistory=true）：
| media_file_id | user_id | submission_time |
|---------------|---------|-----------------|
| songA         | u1      | 2024-01-01 10:00 |
| songA         | u1      | 2024-01-01 11:00 |
| songA         | u1      | 2024-01-01 12:00 |

---

## 四、NowPlaying 临时状态与心跳路径

### 1. NowPlaying 内存状态模型

**核心结构**：`PlaybackSession` - [play_tracker.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/play_tracker.go#L35-L46)

```go
type PlaybackSession struct {
    MediaFile    model.MediaFile
    Start        time.Time      // 开始播放时间
    UserId       string
    Username     string
    PlayerId     string         // 客户端唯一标识
    PlayerName   string
    State        string         // starting/playing/paused/stopped/expired
    PositionMs   int64          // 当前播放位置（毫秒）
    PlaybackRate float64        // 播放速率
    LastReport   time.Time      // 最后一次上报时间
}
```

**存储方式**：`SimpleCache[string, PlaybackSession]` - 内存缓存，key 为 playerId

**TTL 计算**：`remainingTTL` 函数 - [play_tracker.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/play_tracker.go#L252-L259)
- 根据歌曲剩余时长和播放速率计算
- 公式：`(剩余时长 / 播放速率) + 5秒`
- 暂停状态下 TTL 为 30 分钟

### 2. NowPlaying 工作线程

**核心文件**：[nowplaying_worker.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/nowplaying_worker.go)

**工作机制**：

```
enqueueNowPlaying() → npQueue[playerId] = entry
       ↓ (sendNowPlayingSignal)
nowPlayingWorker 每 1 秒检查一次
       ↓
取出所有 npQueue 中的条目
       ↓
dispatchNowPlaying() → 遍历所有 scrobbler 发送 NowPlaying
```

**关键设计**：
- `npQueue` 是 `map[string]nowPlayingEntry`，每个 player 只保留最新状态
- 使用非阻塞 signal（`select default`），避免频繁信号丢失
- 后台线程每秒处理一次，合并短时间内的多次上报

### 3. 心跳路径（ReportPlayback 状态机）

**状态转移**：

```
StateStarting
     ↓
StatePlaying ←→ StatePaused
     ↓                ↓
StateStopped    StateExpired（过期自动触发）
```

**各状态处理**：

| 状态 | 操作 | 数据库写入 | 外部 Scrobble |
|------|------|-----------|---------------|
| `StateStarting` | 创建 PlaybackSession 加入缓存，设置 TTL | ❌ 否 | NowPlaying 更新 |
| `StatePlaying` | 更新缓存位置和 TTL | ❌ 否 | NowPlaying 更新 |
| `StatePaused` | 更新缓存，TTL=30分钟 | ❌ 否 | 视实现而定 |
| `StateStopped` | 判断是否超过阈值，是则 incPlay() | ✅ 是（超过阈值时） | Scrobble 提交 |
| `StateExpired` | 会话过期，自动触发 | ❌ 否 | PlaybackReport |

**ReportPlayback 完整流程**：[play_tracker.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/play_tracker.go#L261-L382)

### 4. 过期会话处理

**缓存过期回调**：[play_tracker.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/play_tracker.go#L136-L148)

```go
m.OnExpiration(func(_ string, info PlaybackSession) {
    if info.State != StateStopped {
        info.State = StateExpired
        info.LastReport = time.Now()
        p.enqueuePlaybackReport(ctx, info)
    }
})
```

- 当播放会话过期（客户端异常退出、网络中断等）时
- 如果会话状态不是 Stopped，会自动转为 Expired
- 通过 playbackReport 通知外部 scrobbler

### 5. GetNowPlaying 查询

**位置**：[play_tracker.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/play_tracker.go#L384-L398)

```go
func (p *playTracker) GetNowPlaying(_ context.Context) ([]PlaybackSession, error) {
    res := p.playMap.Values()
    slices.SortFunc(res, func(a, b PlaybackSession) int {
        return b.Start.Compare(a.Start)
    })
    for i := range res {
        if res[i].State == StatePlaying {
            // 根据最后上报时间和播放速率估算当前位置
            elapsed := time.Since(res[i].LastReport).Milliseconds()
            estimated := res[i].PositionMs + int64(float64(elapsed)*res[i].PlaybackRate)
            res[i].PositionMs = min(estimated, trackDurationMs)
        }
    }
    return res, nil
}
```

- 返回所有正在播放的会话（纯内存查询）
- 对 Playing 状态的会话，根据上次报告时间估算当前播放位置
- 按开始时间倒序排列

---

## 五、Subsonic 接口与心跳路径

### 1. scrobble.view 接口

**位置**：[media_annotation.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/server/subsonic/media_annotation.go#L158-L185)

**参数**：
- `id`：歌曲ID（支持多个）
- `time`：播放时间戳（可选，与 id 一一对应）
- `submission`：是否提交（true=提交播放记录，false=仅更新现在播放）
- `position`：当前播放位置（秒，仅 submission=false 时使用）

**分支逻辑**：
```go
if submission {
    // 提交播放记录 → 写入数据库 + 外部 Scrobble
    err := api.scrobblerSubmit(ctx, ids, times)
} else {
    // 仅更新 NowPlaying → 内存状态 + 外部 NowPlaying 通知
    err := api.scrobblerNowPlaying(ctx, ids[0], position)
}
```

### 2. submission=false 的心跳路径

**scrobblerNowPlaying 实现**：[media_annotation.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/server/subsonic/media_annotation.go#L203-L229)

```
scrobble.view?submission=false&id=xxx&position=123
    ↓
scrobblerNowPlaying()
    ↓
查询 MediaFile 信息
    ↓
ReportPlayback(StatePlaying, positionMs=position*1000)
    ↓
    ├─→ 更新内存 playMap（TTL = 剩余时长）
    ├─→ enqueuePlaybackReport() → 外部 scrobbler PlaybackReport
    └─→ enqueueNowPlaying() → 外部 scrobbler NowPlaying
```

**关键特性**：
- **不写入数据库**：不会增加 play_count，不会更新 play_date
- **纯内存操作**：仅更新 playMap 缓存
- **外部通知**：会通知外部 scrobbler 更新 NowPlaying 状态
- **支持心跳**：客户端定期调用 submission=false 维持 NowPlaying 状态

### 3. submission=true 的提交路径

**scrobblerSubmit 实现**：[media_annotation.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/server/subsonic/media_annotation.go#L187-L201)

```
scrobble.view?submission=true&id=xxx&time=1234567890
    ↓
scrobblerSubmit()
    ↓
Submit(submissions)
    ↓
incPlay() → 写入 annotation 和 scrobbles 表
    ↓
dispatchScrobble() → 外部 scrobbler 缓冲提交
```

**支持批量提交**：一次可提交多首歌曲的 scrobble

**时间戳支持**：
- 支持传入 `time` 参数（Unix 时间戳）
- 这意味着可以提交**历史播放记录**
- 这也是 `play_date` 使用 `max()` 的重要原因之一（见第七章分析）

### 4. ReportPlayback 接口（Native API）

**位置**：[media_annotation.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/server/subsonic/media_annotation.go#L231-L290)

更丰富的状态上报接口，支持：
- `mediaId` / `mediaType`：媒体ID和类型
- `positionMs`：播放位置（毫秒精度）
- `state`：播放状态（starting/playing/paused/stopped）
- `playbackRate`：播放速率
- `ignoreScrobble`：是否忽略 scrobble（用于预览等场景）

---

## 六、外部 Scrobble 机制（Last.fm 等）

### 1. Scrobbler 接口定义

**位置**：[interfaces.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/interfaces.go#L22-L27)

```go
type Scrobbler interface {
    IsAuthorized(ctx context.Context, userId string) bool
    NowPlaying(ctx context.Context, userId string, track *model.MediaFile, position int) error
    Scrobble(ctx context.Context, userId string, s Scrobble) error
    PlaybackReport(ctx context.Context, info PlaybackSession) error
}
```

### 2. 内置与插件 Scrobbler

**两种来源**：
1. **内置 Scrobbler**：如 Last.fm，在代码中直接实现
   - 注册方式：`scrobbler.Register(name, constructor)`
   - 初始化时创建，存储在 `builtinScrobblers`

2. **插件 Scrobbler**：通过插件系统加载
   - 动态加载/卸载
   - 存储在 `pluginScrobblers`
   - 每次调用前刷新（`refreshPluginScrobblers`）

**统一访问**：`getActiveScrobblers()` - 返回合并后的 map

### 3. 缓冲 Scrobbler 机制

**核心文件**：[buffered_scrobbler.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/buffered_scrobbler.go)

**设计目的**：
- 外部服务可能不可用（网络问题、服务宕机）
- 需要重试机制保证播放记录不丢失
- 解耦主流程和外部调用

**架构**：

```
dispatchScrobble()
    ↓
bufferedScrobbler.Scrobble()
    ↓
ScrobbleBuffer.Enqueue() → 写入 scrobble_buffer 表
    ↓
sendWakeSignal()
    ↓
后台 run() 协程处理队列
    ↓
processUserQueue()
    ↓
底层 Scrobbler.Scrobble() → 发送到外部服务
    ↓
成功 → Dequeue() 从缓冲表删除
失败 → ErrRetryLater 则保留，下次重试
       ErrUnrecoverable 则丢弃
```

**两种构造方式**：
- `newBufferedScrobbler`：包装静态实例（内置 scrobbler）
- `newBufferedScrobblerWithLoader`：包装动态加载器（插件 scrobbler）

### 4. Last.fm Scrobbler 实现

**核心文件**：[agent.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/adapters/lastfm/agent.go)

**NowPlaying 实现**（第 357-377 行）：
- 调用 Last.fm API 的 `track.updateNowPlaying`
- 实时发送，不经过缓冲区
- 失败返回 `ErrUnrecoverable`，不重试

**Scrobble 实现**（第 379-412 行）：
- 调用 Last.fm API 的 `track.scrobble`
- 经过 bufferedScrobbler 缓冲
- 时长 <= 30 秒的歌曲跳过（Last.fm 规则）
- 错误处理：
  - 错误码 11（服务暂时不可用）或 16（临时错误）→ `ErrRetryLater` 重试
  - 其他错误 → `ErrUnrecoverable` 丢弃

**配置项**：
- `LastFM.Enabled`：是否启用
- `LastFM.ApiKey` / `LastFM.Secret`：API 密钥
- `LastFM.ScrobbleFirstArtistOnly`：是否只 Scrobble 第一位艺术家
- `LastFM.Languages`：语言偏好

### 5. ScrobbleBuffer 数据库表

**位置**：[scrobble_buffer_repository.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/scrobble_buffer_repository.go)

**核心方法**：
- `Enqueue()`：入队，插入记录
- `Next()`：获取下一条待处理记录（按 play_time, rowid 排序）
- `Dequeue()`：出队，删除记录
- `UserIDs()`：获取所有有未处理记录的用户ID列表
- `Length()`：获取队列总长度

**注意**：这与 `scrobbles` 表是**两张不同的表**：
- `scrobble_buffer`：外部 Scrobble 待发送队列
- `scrobbles`：本地完整播放历史记录

---

## 七、最近播放列表展示链路（深度分析）

### 1. 白名单注册与 LEFT JOIN 列的配合

**这是理解 play_date 排序如何工作的关键**。

#### 7.1.1 白名单注册机制

**位置**：[sql_restful.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/sql_restful.go#L144-L165)

```go
func registerFieldWhiteList(name string, instance any) {
    // 1. 注册模型本身的所有字段
    m := structs.Map(instance)
    whiteList[name] = map[string]struct{}{}
    for k := range m {
        whiteList[name][toSnakeCase(k)] = struct{}{}
    }
    // 2. ★ 关键：同时注册 model.Annotations 的所有字段
    ma := structs.Map(model.Annotations{})
    for k := range ma {
        whiteList[name][toSnakeCase(k)] = struct{}{}
    }
}
```

**设计精妙之处**：
- 每个仓库的白名单不仅包含自身模型字段
- 还包含 `model.Annotations{}` 的所有字段
- 这意味着 `play_date`、`play_count`、`rating`、`starred`、`starred_at` 自动被加入白名单
- 无需为每个仓库手动声明 annotation 相关的排序/过滤白名单

#### 7.1.2 model.Annotations 字段

**位置**：[annotation.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/model/annotation.go#L27-L37)

```go
type Annotations struct {
    Starred    bool      `json:"starred"`
    StarredAt  time.Time `json:"starredAt"`
    Rating     int       `json:"rating"`
    PlayCount  int       `json:"playCount"`
    PlayDate   time.Time `json:"playDate"`
    BookmarkPosition int64 `json:"bookmarkPosition,omitempty"`
    BookmarkComment  string `json:"bookmarkComment,omitempty"`
    BookmarkUpdated  time.Time `json:"bookmarkUpdated,omitempty"`
}
```

对应的 snake_case 字段名：`starred`, `starred_at`, `rating`, `play_count`, `play_date` 等。

#### 7.1.3 withAnnotation LEFT JOIN

**位置**：[sql_annotations.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/sql_annotations.go#L18-L45)

```go
func (r sqlRepository) withAnnotation(query SelectBuilder, idField string) SelectBuilder {
    userID := loggedUser(r.ctx).ID
    query = query.
        LeftJoin("annotation on (" +
            "annotation.item_id = " + idField +
            " AND annotation.user_id = '" + userID + "')").
        Columns(
            "annotation.starred",
            "annotation.starred_at",
            "annotation.rating",
            "annotation.play_count",
            "annotation.play_date",
        )
    return query
}
```

**配合关系**：

```
白名单注册 (registerFieldWhiteList)
    ↓ 注入 play_date 等字段到白名单
REST API sanitizeSort
    ↓ play_date 在白名单中，允许排序
SQL 查询构建 (withAnnotation)
    ↓ LEFT JOIN annotation 表，查询出 play_date 列
SQL 排序 (ORDER BY play_date)
    ↓ play_date 是查询结果中的列，可以直接排序
```

### 2. 排序处理完整流程

#### 7.2.1 sortMapping 映射

**位置**：[sql_base_repository.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/sql_base_repository.go#L120-L163)

```go
func (r sqlRepository) sortMapping(sort string) string {
    // 1. 精确匹配
    if mapping, ok := r.sortMappings[sort]; ok {
        return mapping
    }
    // 2. camelCase 匹配
    if mapping, ok := r.sortMappings[toCamelCase(sort)]; ok {
        return mapping
    }
    // 3. snake_case 匹配
    sort = toSnakeCase(sort)
    if mapping, ok := r.sortMappings[sort]; ok {
        return mapping
    }
    // 4. 无映射，直接使用字段名
    return sort
}
```

#### 7.2.2 REST API 排序白名单检查

**位置**：[sql_restful.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/sql_restful.go#L69-L80)

```go
func (r sqlRepository) sanitizeSort(sort, order string) (string, string) {
    if sort != "" {
        sort = toSnakeCase(sort)
        if mapped, ok := r.sortMappings[sort]; ok {
            sort = mapped
        } else {
            // ★ 关键：不在 sortMappings 中时，检查字段白名单
            if !r.isFieldWhiteListed(sort) {
                log.Warn(r.ctx, "Ignoring sort not whitelisted", "sort", sort)
                sort = ""
            }
        }
    }
    return sort, order
}
```

**执行流程**：
1. 转换为 snake_case
2. 查找 `sortMappings`，有映射则使用映射结果
3. 无映射则检查白名单
4. 在白名单中 → 直接使用该字段名排序
5. 不在白名单中 → 忽略排序（安全机制，防止 SQL 注入）

#### 7.2.3 play_date 排序实际生效过程

以专辑列表按 `play_date` 排序为例：

```
前端: GET /api/album?sort=play_date&order=DESC
    ↓
sanitizeSort("play_date", "DESC")
    ↓
toSnakeCase("play_date") → "play_date"
    ↓
sortMappings["play_date"] → 不存在
    ↓
isFieldWhiteListed("play_date") → true（因为注册了 model.Annotations）
    ↓
sort = "play_date"
    ↓
selectAlbum() → withAnnotation() → 查询出 play_date 列
    ↓
ORDER BY play_date DESC
    ↓
返回结果，按 play_date 降序排列
```

### 3. 专辑级最近播放

**前端请求**（来自 albumLists.jsx）：
```
GET /api/album?sort=play_date&order=DESC&filter={"recently_played":true}
```
[albumLists.jsx](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/ui/src/album/albumLists.jsx#L66-L75)

**后端处理流程**：

1. **路由接收**：[native_api.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/server/nativeapi/native_api.go#L69)

2. **查询构建**：[album_repository.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/album_repository.go#L219-L224)

3. **Annotation 关联**：[sql_annotations.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/sql_annotations.go#L18-L45)

4. **过滤器处理**：
   - `recently_played` 过滤器 → `play_count > 0`

**Subsonic API 对应接口**：
- `getAlbumList?type=recent` → [album_lists.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/server/subsonic/album_lists.go#L30-L31)
- 使用 `filter.AlbumsByRecent()` → `Sort: "playDate", Order: "desc", Filters: Gt{"play_date": time.Time{}}`

### 4. 歌曲级最近播放

**前端请求**（用户手动选择 play_date 列排序）：
```
GET /api/song?sort=play_date&order=DESC
```
[SongList.jsx](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/ui/src/song/SongList.jsx#L152)

**后端处理流程**：

1. **查询构建**：[mediafile_repository.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/mediafile_repository.go#L171-L177)

2. **排序生效**：
   - `play_date` 不在 `sortMappings` 中
   - 但在白名单中（因为注册了 model.Annotations）
   - 直接作为列名用于 ORDER BY

---

## 八、深度设计决策分析

### 1. 为什么 play_date 使用 max() 而不是直接覆盖？

**核心代码**：[sql_annotations.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/sql_annotations.go#L116-L137)

```go
Set("play_date", Expr("max(ifnull(play_date,''),?)", ts))
```

#### 8.1.1 原因一：Subsonic Submit 支持历史时间戳

Subsonic `scrobble.view?submission=true` 接口支持传入 `time` 参数：
- 客户端可以提交**任意时间**的播放记录
- 时间顺序可能是乱序的（先提交 T1，再提交 T0，T0 < T1）
- 如果直接覆盖，旧时间戳会把 `play_date` 拉回到更早的时间

**场景示例**：
```
时间线真实播放顺序：T1 → T2 → T3
客户端提交顺序：T2 → T1 → T3

如果直接覆盖 play_date：
  提交 T2 → play_date = T2 ✓
  提交 T1 → play_date = T1 ✗ (被更早的时间覆盖了！)
  提交 T3 → play_date = T3 ✓

使用 max()：
  提交 T2 → play_date = max(NULL, T2) = T2 ✓
  提交 T1 → play_date = max(T2, T1) = T2 ✓ (保持最新)
  提交 T3 → play_date = max(T2, T3) = T3 ✓
```

#### 8.1.2 原因二：客户端上报时序不保证

- 网络延迟：先播放的歌曲可能后到达服务器
- 多设备同步：不同设备的播放记录同步顺序不确定
- 离线播放：客户端离线缓存后批量上传，顺序可能混乱
- `play_date` 的语义是"最后一次播放时间"，不是"最后一次上报时间"

#### 8.1.3 原因三：保护最近播放列表的正确性

最近播放列表按 `play_date` 降序排列：
- 如果 play_date 可能被旧时间覆盖，列表顺序会混乱
- 用户刚听过的歌可能突然从"最近播放"列表顶部消失
- 使用 max() 保证 `play_date` 单调不减，列表顺序只会越来越新

#### 8.1.4 代价：无法处理"删除播放记录"场景

max() 语义的局限：
- 如果用户想"删除"某条播放记录，无法简单实现
- 但 Navidrome 目前没有提供删除播放历史的功能
- 对"最近播放"场景来说，max() 是正确且高效的选择

### 2. 为什么 scrobbles 表不开放查询 API？

#### 8.2.1 设计目标分离

| 表 | 设计目标 | 访问模式 |
|----|---------|---------|
| `annotation` | **展示优化**：快速查询、排序、过滤 | 读多写少 |
| `scrobbles` | **记录优化**：完整保留每次播放 | 写多读少 |

**annotation 表优势**：
- 数据量小（每用户每资源一条）
- 索引简单（user_id + play_date）
- 查询性能好，适合列表展示
- 天然去重，用户体验好

**scrobbles 表劣势**：
- 数据量大（每次播放一条）
- 如果开放查询，需要考虑分页、过滤、排序等
- 性能开销大，可能影响主要功能

#### 8.2.2 当前阶段定位

scrobbles 表目前是**数据收集层**：
- 主要用于内部统计和分析（如 insights 模块）
- 参考：[insights data.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/metrics/insights/data.go)
- 尚未设计面向用户的播放历史详情页
- 先收集数据，再考虑如何展示

#### 8.2.3 与 annotation 的功能重叠

对于"最近播放"场景：
- annotation 表已经能满足需求（按最后播放时间排序）
- 用户通常不需要看到"同一首歌重复出现"的历史列表
- 开放 scrobbles 查询可能造成用户困惑（为什么一首歌出现多次？）

#### 8.2.4 预留扩展空间

不开放 API 不等于没用：
- 为未来功能预留数据基础（播放统计、听歌报告、年度总结等）
- 数据先收集起来，等需要时再设计展示方式
- 避免早期开放 API 后被依赖，后续难以修改

#### 8.2.5 安全与隐私考虑

完整播放历史是敏感数据：
- 包含用户详细的听歌行为模式
- 开放查询接口增加数据泄露风险
- 目前仅内部使用，攻击面更小

### 3. 为什么要有 scrobble_buffer 表（外部 Scrobble 缓冲）？

**与 scrobbles 表的区别**：
- `scrobbles`：**本地**历史记录，永久保存
- `scrobble_buffer`：**外部**发送队列，发送成功后删除

**设计原因**：
1. **网络不可靠**：外部服务（Last.fm）可能暂时不可用
2. **重试机制**：需要在后台重试，不阻塞主流程
3. **持久化队列**：服务重启后不丢失待发送记录
4. **解耦**：播放计数更新和外部 Scrobble 是两个独立操作

---

## 九、核心代码文件速查

### 写入链路
| 模块 | 文件 | 关键函数 |
|------|------|----------|
| 播放跟踪 | [play_tracker.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/play_tracker.go) | `ReportPlayback`, `incPlay`, `Submit`, `GetNowPlaying` |
| NowPlaying Worker | [nowplaying_worker.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/nowplaying_worker.go) | `enqueueNowPlaying`, `nowPlayingWorker`, `dispatchNowPlaying` |
| PlaybackReport Worker | [playbackreport_worker.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/playbackreport_worker.go) | `enqueuePlaybackReport`, `playbackReportWorker` |
| Annotation 更新 | [sql_annotations.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/sql_annotations.go) | `IncPlayCount`, `withAnnotation` |
| Scrobble 历史记录 | [scrobble_repository.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/scrobble_repository.go) | `RecordScrobble` |
| Scrobble 缓冲队列 | [scrobble_buffer_repository.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/scrobble_buffer_repository.go) | `Enqueue`, `Next`, `Dequeue` |
| 缓冲 Scrobbler | [buffered_scrobbler.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/core/scrobbler/buffered_scrobbler.go) | `Scrobble`, `processQueue`, `processUserQueue` |
| Last.fm 实现 | [agent.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/adapters/lastfm/agent.go) | `NowPlaying`, `Scrobble` |
| Subsonic API | [media_annotation.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/server/subsonic/media_annotation.go) | `Scrobble`, `ReportPlayback`, `scrobblerSubmit`, `scrobblerNowPlaying` |

### 展示链路
| 模块 | 文件 | 关键函数 |
|------|------|----------|
| 专辑查询 | [album_repository.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/album_repository.go) | `selectAlbum`, `GetAll` |
| 歌曲查询 | [mediafile_repository.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/mediafile_repository.go) | `selectMediaFile`, `GetAll` |
| 白名单注册 | [sql_restful.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/sql_restful.go) | `registerModelWhiteList`, `sanitizeSort` |
| 过滤器 | [filters.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/server/subsonic/filter/filters.go) | `AlbumsByRecent` |
| 通用排序 | [sql_base_repository.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/persistence/sql_base_repository.go) | `sortMapping`, `buildSortOrder` |

### 数据模型
| 模型 | 文件 |
|------|------|
| Scrobble | [scrobble.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/model/scrobble.go) |
| ScrobbleBuffer | [scrobble_buffer.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/model/scrobble_buffer.go) |
| Annotation | [annotation.go](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/model/annotation.go) |
| 数据库迁移 | [20251206013022_create_scrobbles_table.sql](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/db/migrations/20251206013022_create_scrobbles_table.sql) |

### 前端
| 页面 | 文件 |
|------|------|
| 专辑列表 | [albumLists.jsx](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/ui/src/album/albumLists.jsx) |
| 歌曲列表 | [SongList.jsx](file:///d:/fz/0601-1/solo-dogfeeding/code/73-navidrome/ui/src/song/SongList.jsx) |

---

## 十、关键设计决策总结

### 存储层
1. **双轨制存储**：annotation 表用于展示（去重），scrobbles 表用于历史记录（不去重），两者通过事务保证一致性

2. **play_date 的 max() 语义**：
   - 防止乱序时间戳破坏排序
   - 适配 Subsonic Submit 的历史时间戳提交
   - 保证最近播放列表的正确性

3. **三表分离**：
   - `annotation`：用户标注，用于展示
   - `scrobbles`：本地播放历史，用于统计分析
   - `scrobble_buffer`：外部 Scrobble 发送队列，用于重试

### 架构层
4. **LEFT JOIN annotation**：通过 LEFT JOIN 将用户标注注入查询结果，配合白名单机制，使得 `play_date` 可以像普通字段一样用于过滤和排序

5. **白名单自动扩展**：注册模型白名单时自动加入 `model.Annotations` 的所有字段，无需手动维护

6. **缓冲 Scrobbler 模式**：外部 Scrobble 通过数据库缓冲队列异步发送，支持重试，不阻塞主流程

### 业务逻辑层
7. **层级联动更新**：播放歌曲时同时更新歌曲、专辑、艺术家的播放计数和最后播放时间，通过事务保证原子性

8. **播放阈值判定**：必须播放超过 50% 或 4 分钟才算有效播放，防止误触产生大量无效记录

9. **内存状态管理**：NowPlaying 状态完全在内存中管理，通过 TTL 自动过期，不写入数据库

10. **双路径上报**：
    - Subsonic `scrobble.view?submission=false`：心跳路径，仅更新内存状态和外部 NowPlaying
    - Subsonic `scrobble.view?submission=true`：提交路径，写入数据库和外部 Scrobble
    - Native `reportPlayback`：更丰富的状态机（starting/playing/paused/stopped）
